all:
  children:
    edge:
      hosts:
        homelab-edge:
          ansible_host: "{{ lookup('env', 'EDGE_IP') }}"
          ansible_user: matt
          ansible_become: true
          ansible_ssh_private_key_file: "{{ lookup('env', 'HOME') }}/.ssh/homelab-edge"
